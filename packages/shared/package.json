{
  "name": "@media-ops/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, utils, constants dùng chung giữa web và api",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
